export async function findActiveWithdrawal(_clientOrDb, _userId) {
  return null;
}

export async function assertNoActiveWithdrawal(_clientOrDb, _userId) {
  // Multiple withdrawals allowed as long as user has sufficient balance
}
